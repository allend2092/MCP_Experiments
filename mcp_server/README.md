# MCP Server

This package contains the MCP server entrypoint and tool definitions.

Design goals:
- Safe-by-default (read-only tools)
- Explicit host and command allowlists
- Portable deployment (Pi, VM, container)
